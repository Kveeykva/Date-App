import React from "react";
import { View, Text } from "react-native";
import styles from "./styles";

const CurrentLocation = (props) => {
  return (
    <View>
      <Text> Location Current </Text>
    </View>
  );
};
export default CurrentLocation;
